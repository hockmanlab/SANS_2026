## About Seurat

[Seurat documentation](https://satijalab.org/seurat/)

**Seurat** is an R package designed for the quality control, analysis, and exploration of single-cell RNA-seq data. It provides a range of tools for processing single-cell datasets and identifying biological patterns and sources of variation between cells.

In this tutorial, Seurat will be used to take the gene expression matrix generated by Cell Ranger and perform downstream analysis, including **quality control, normalization, dimensionality reduction, clustering, visualization, identification of marker genes, label transfer and differential gene expression analysis**.

Before starting the analysis, we need to install and load the required R packages. Loading these packages gives us access to the functions and tools used throughout the workflow and ensures that all required dependencies are available.

### Installing the required packages

The following code checks whether each package is already installed. If a package is missing, it will be installed automatically. This allows the script to be run again without unnecessarily reinstalling packages.

# Installation

Before starting the workshop, please install **R** and **RStudio** on your computer.

## 1. Install R

Download and install R from the official R Project website:

👉 **[Download R](https://www.r-project.org/)**

Once you are on the **R Project for Statistical Computing** page, click the bold **download R** hyperlink. This will take you to the **CRAN Mirrors** page.

CRAN (Comprehensive R Archive Network) provides the files needed to download and install R. The CRAN page may recommend selecting a mirror that is geographically close to you. However, this is not essential. If you are unsure which mirror to select, you can use the **[Worldwide option](https://mirrors.cicku.me/cran/)**.

When downloading R, **select the version that matches your operating system**:

- **macOS** — download the macOS version
- **Windows** — download the Windows version
- **Linux** — download the version for your Linux distribution

> **Important:** Make sure you download the correct version of R for your operating system.

## 2. Install RStudio

After installing R, download and install **RStudio Desktop** from Posit:

👉 **[Download RStudio](https://posit.co/downloads/)**

On the download page, select **RStudio IDE**. For this workshop, you will be using RStudio locally on your own computer.

Select the installer that matches your operating system:

- **macOS** — select the macOS installer
- **Windows** — select the Windows installer
- **Linux** — select the Linux installer

> **Important:** Install **R first**, followed by **RStudio**.

### Running code in RStudio

If you are new to RStudio, you will generally work with R code in the **Source** panel.

To open the Source panel, click the icon at the top of the RStudio window showing **two overlapping windows**. You can then paste or type your R code into the Source panel.

To run a line of code:

1. Place your cursor on the line you want to run.
2. Click the **Run** button, or
3. Use **Command + Enter** on macOS or **Ctrl + Enter** on Windows.

You can also select multiple lines of code and run them together.

### Updating packages in RStudio

If you already have RStudio installed and are using an older version, RStudio may ask whether you want to update packages and provide options such as **all / some / none**.

If you are unsure which option to select, type:

```text
a
```

into the Console and press Enter. This selects **all** packages for updating.

## 3. Install Required R Packages

Once R and RStudio are installed, open RStudio and run the following code:

```r
# Increase the download timeout for large package dependencies
options(timeout = 1800)

# Install required CRAN packages
cran_pkgs <- c(
  "Seurat",
  "SeuratObject",
  "ggplot2",
  "plyr",
  "dplyr",
  "patchwork",
  "future",
  "hdf5r",
  "BiocManager",
  "sctransform",
  "HGNChelper",
  "scales",
  "openxlsx",
  "data.table",
  "R.utils",
  "leidenbase",
  "remotes"
)

new_cran <- cran_pkgs[
  !cran_pkgs %in% installed.packages()[, "Package"]
]

if (length(new_cran) > 0) {
  install.packages(new_cran)
}

# Install glmGamPoi if not already installed
if (!requireNamespace("glmGamPoi", quietly = TRUE)) {
  BiocManager::install("glmGamPoi")
}
# GitHub packages
if (!requireNamespace("SeuratWrappers", quietly = TRUE)) {
  remotes::install_github(
    "satijalab/seurat-wrappers",
    upgrade = "never"
  )
}

if (!requireNamespace("Azimuth", quietly = TRUE)) {
  remotes::install_github(
    "satijalab/azimuth",
    ref = "master",
    upgrade = "never"
  )
}
```
You may get a couple message asking "Do you want to install from sources the packages which need compilation? (Yes/no/cancel)" **and**

Do you want to attempt to install these from sources? (Yes/no/cancel)"


If you are unsure which option to select, type:

```text
Yes
```
for both.

> **Note:** `plyr` is included because some users may encounter issues loading or using `dplyr` if `plyr` has not been loaded first.

The code first creates a list of the required packages. It then checks which packages are already installed and installs only those that are missing. This means you can run the installation code again without unnecessarily reinstalling packages.

`glmGamPoi` is installed separately using **BiocManager** because it is a Bioconductor package rather than a standard CRAN package.

## 4. Check Your Installation

Run the following code to make sure all required packages are available:

```r
library(Seurat)
library(SeuratObject)
library(ggplot2)
library(plyr)
library(dplyr)
library(patchwork)
library(future)
library(hdf5r)
library(glmGamPoi)
library(R.utils)
library(leidenbase)
library(HGNChelper)
library(scales)
library(Azimuth)
```

If all packages load without errors, you are ready for the workshop! 🧬

> **Note:** It is normal to see some warning messages when installing or loading packages. Warnings do not necessarily mean that something has gone wrong. If you encounter an **error**, however, this indicates that something needs to be addressed.

Each package provides functionality that will be used during the analysis. For example, **Seurat** provides the main single-cell analysis functions, **ggplot2** is used for visualization, **dplyr** and **plyr** provide tools for manipulating data, **patchwork** allows multiple plots to be combined into a single figure, and **hdf5r** allows R to work with HDF5 files such as the Cell Ranger output used in this workshop.

### Setting a seed

Finally, we set a random seed:

```r
# Reproducibility
set.seed(42)
```

Setting a seed helps make analyses involving random processes reproducible. For example, some steps may involve random sampling or initialization. Using the same seed means that, where possible, the same analysis will produce the same result when the code is run again.

With the required packages installed and loaded, we can now begin working with the **Cell Ranger output and create our Seurat object**.
## 5. Download the Tutorial Dataset

This workshop uses a real 10x Genomics Cell Ranger output file, **`filtered_feature_bc_matrix.h5`**, so that everyone works with the same data throughout.

### Dataset details

| Detail | Value |
|---|---|
| **Sample** | Kramnik mouse |
| **Condition** | Saline |
| **Timepoint** | Day 17 |
| **Extraction site** | Brain |
| **Reference genome** | GRCm39 (mouse) |
| **Chemistry** | 10x Genomics Single Cell 3′ v3 |
| **Cell Ranger version** | 8.0.0 |
| **Matrix dimensions** | 33,696 features × 8,601 cells |
| **File size** | ~28 MB |

The file is the **filtered** feature-barcode matrix produced by `cellranger count`, stored in HDF5 format.

The filtered matrix contains the genes/features and cell barcodes retained by Cell Ranger after its initial processing and filtering. This is the file that will be loaded into Seurat for downstream single-cell analysis.

### Downloading the file

Download `filtered_feature_bc_matrix.h5` from the repo's Releases page:

👉 **[Download filtered_feature_bc_matrix.h5](https://github.com/hockmanlab/SANS_2026/releases/download/data/filtered_feature_bc_matrix.1.h5)**

Save it somewhere memorable on your computer, such as a `data/` folder alongside this repo.

> **Note:** Since this repo is currently private, you'll need to be logged into a GitHub account with access to the repo for this link to work.

> **Note:** The file is not stored directly in this GitHub repository due to its size. Keep track of the full path to wherever you save it — you'll need it in [2.2 Seurat Exploration](2.2_Seurat_exploration.md) when setting `DATA_FILE`.

> **Important:** When creating a new folder to store the HDF5 file, avoid using spaces in the folder or file path. Use underscores (`_`) instead. For example, use `SANS_2026/data` rather than `SANS 2026/data`. Avoiding spaces makes it easier to specify file paths in R and can prevent unnecessary path-related errors later in the analysis.

### Finding the file path

You will need the full path to the downloaded HDF5 file when setting `DATA_FILE`.

**On macOS:**

1. Locate the downloaded file in Finder.
2. Right-click the file and select **Get Info**.
3. Under **General**, look for **Where:**.
4. This shows the location of the file.

Copying the path from **Get Info** is useful because it helps account for details such as files stored in iCloud Drive or spaces in folder names that may be difficult to notice when typing the path manually.

For example, a file may be stored somewhere within your iCloud Drive. Copying the path directly from **Get Info** helps ensure that the complete path is entered correctly.

**On Windows:**

You can find the directory path by locating the file in **File Explorer** and using the address bar at the top of the window. You can also right-click the file, select **Properties**, and use the location shown there to identify the directory containing the file.

> **Tip:** Copying the path rather than typing it manually helps prevent small path errors, including missing spaces or incorrect folder names.

### Verifying the download

Once downloaded, you can confirm that the file exists before starting the workshop.

If you are copying and running the code from this page from top to bottom, you **do not need to run `library(hdf5r)` again** because it was already loaded in the package-loading section above.

Set `DATA_FILE` to the location of your downloaded HDF5 file:

```r
#library(hdf5r)
DATA_FILE <- "/path/to/filtered_feature_bc_matrix.1.h5"

# Confirm the file exists
file.exists(DATA_FILE)
```

> **Important:** Replace only `/path/to/` with the path to the folder containing your file. Keep the quotation marks (`" "`) and the `.h5` file extension.

For example:

```r
DATA_FILE <- "/Users/yourname/Desktop/SANS_2026/data/filtered_feature_bc_matrix.1.h5"
```

The exact path will be different for each computer.

**Expected output:**

```text
[1] TRUE
```

If `file.exists()` returns `TRUE`, the file downloaded correctly and you are ready to load it into Seurat in the next section.

> **Note:** Depending on how the file was downloaded, the filename may appear as **`filtered_feature_bc_matrix.1.h5`** rather than **`filtered_feature_bc_matrix.h5`**. This extra `.1` does not prevent the HDF5 file from being read. If your downloaded file has `.1` in the filename, make sure the filename in `DATA_FILE` exactly matches the file on your computer. Alternatively, you can rename the file to `filtered_feature_bc_matrix.h5`.

> **Note:** If you see warning messages during installation or while loading packages, do not worry immediately. Warnings can be normal. Errors are the messages that indicate something has gone wrong and may need to be addressed.

Note, for Section 3: Clustering and Annotation we will be utilizing a preprocessed .RDS file which will ensure that all the participants are on the same page for the final part of the workshop to reinforce consistency. A .RDS file is R's native format for saving a single R object to disk exactly as it exists in memory - including its full internal structure, not just the raw data values. Thus, the .RDS file captures the object as is when you create and save it. This way, when you load it into R you can begin where you left off without, with all the prior steps already incorporated. 

(**You can download this file now, but will only be used in Section 3**)
Save it to a location you will remember as you will need to guide Seurat to the path to the file itself when we load it.

The pre-processed Seurat object can be downloaded here:

[Download the pre-processed Seurat
object](https://github.com/hockmanlab/SANS_2026/releases/download/SCTransform_preprocessed_data/seurat_preprocessed_SCT.rds)

With the required packages installed and loaded, and the tutorial dataset downloaded and verified, we can now begin working with the **Cell Ranger output and create our Seurat object**.
